# =============================================================================
# AULA VIRTUAL - BACKEND CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to git!


# =============================================================================
# DATABASE
# =============================================================================
DATABASE_URL=mysql+pymysql://root:root@db:3306/aulavirtualbd
# For production, use strong passwords and restrict access
# Example: mysql+pymysql://app_user:STRONG_PASSWORD@mysql-host:3306/production_db

# Timezone for database timestamps (format: +HH:MM or -HH:MM)
# Europe/Madrid = +02:00 (CET/CEST)
TIMEZONE=+02:00


# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
# CRITICAL: Generate a strong random secret (minimum 32 characters)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_32_CHARS

# JWT settings
JWT_ALG=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60


# =============================================================================
# AWS BEDROCK (AI Service)
# =============================================================================
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=amazon.nova-micro-v1:0

# AWS Credentials
# IMPORTANT: Use IAM roles in production, not access keys!
# For development only:
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
# AWS_SESSION_TOKEN=  # Only needed for temporary credentials


# =============================================================================
# RATE LIMITING (Optional - defaults in code)
# =============================================================================
# Default limits are configured in app/main.py
# AI Reply: 15 requests per minute
# PDF Generation: 3 requests per hour


# =============================================================================
# NOTES
# =============================================================================
# - Do NOT use quotes around values
# - Do NOT use 'export' keyword
# - Keep this file secure and never commit to version control
# - In production, use environment-specific secrets management (AWS Secrets Manager, etc.)
